'use babel';

import SlotGampangMaxwinView from '../lib/slot-gampang-maxwin-view';

describe('SlotGampangMaxwinView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
